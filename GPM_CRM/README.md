# GPM CRM

Projeto isolado de CRM para a equipe GPM.

## Estrutura

- `backend/` - Node.js + Express + SQLite
- `frontend/` - React + Vite + Tailwind CSS
- `backend/data/` - banco SQLite local

## Como usar

1. Abra um terminal em `GPM_CRM`
2. Instale dependências:
   - `cd backend && npm install`
   - `cd ../frontend && npm install`
3. Inicie o backend:
   - `cd backend && npm start`
4. Inicie o frontend:
   - `cd frontend && npm run dev`

## Observação

- A planilha de origem está em `CRM/FUNIL_MDO_corrigido.xlsx` fora desta pasta.
- O site institucional existente em `index.html` no nível superior não foi alterado.
